import { ExamennaamFilterPipe } from './examennaam-filter.pipe';

describe('ExamennaamFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExamennaamFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
